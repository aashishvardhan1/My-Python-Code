This directory holds all the topics after modules and packages.

Here built in modules are discussed
to refer all the built in functions  search for 'python 3 module index' in your browser.

There are so many built-in modules in python. An example is given in RandomValues.py